# McpKubernetes

Servidor MCP somente leitura para Kubernetes.
Instale com `dotnet tool install --global McpKubernetes` e use o comando `mcp-kubernetes`.

Variáveis: `KUBECONFIG`, `K8S_CONTEXT`, `K8S_NAMESPACE`, `K8S_MAX_LOG_LINES`, `K8S_READONLY=true`.
